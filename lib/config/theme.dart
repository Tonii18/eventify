import 'package:flutter/material.dart';

class AppColors {

  // Primary Colors

  static const primaryPurple = Color.fromRGBO(97, 92, 233, 1.0);
  static const primaryOrange = Color.fromRGBO(252, 149, 54, 1.0);
  static const darkBlue = Color.fromRGBO(63, 61, 86, 1.0);
  static const white = Colors.white;
  static const greyBackground = Color.fromRGBO(240, 240, 240, 1.0);

  // Seconday Colors

  static const redAccent = Colors.redAccent;
  static const greenAccent = Colors.greenAccent;
  static const orangeAccent = Colors.orangeAccent;
  static const secondaryPurple = Color.fromRGBO(55, 52, 131, 1.0);
  static const lightGrey = Color.fromRGBO(180, 180, 180, 1.0);
  
}